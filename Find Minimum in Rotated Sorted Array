Brute Force Solution:

class Solution:
    def findMin(self, nums: List[int]) -> int:
        res = nums[0]
        for num in nums:
            if num < res:
                res = num
        return res
O(N), O(1)

