class Solution:
    def divideArray(self, nums: List[int]) -> bool:
        dic = defaultdict()
        for num in nums:
            dic[num] = dic.get(num, 0) + 1
        for num, count in dic.items():
            if count % 2 != 0: 
                return False
        return True
